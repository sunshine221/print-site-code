# 需求卡片：FE-004 询价/联系（双路径）

## 基本信息
- 编号：FE-004
- 名称：询价/联系（双路径）
- 状态：draft
- 关联文档：
  - PRD：`.trae/documents/3D打印产品展示与管理平台-PRD.md`
  - 技术架构：`.trae/documents/3D打印产品展示与管理平台-技术架构.md`

## 背景与目标
- 背景：询价是核心转化闭环，需要覆盖“代打服务（上传文件）”与“平台产品（选 SKU，无需上传）”两条路径，并产出可跟进的线索。
- 目标用户/使用场景：访客在公开端提交询价；运营/管理员在后台跟进处理。
- 业务目标（可量化）：提高询价提交成功率，缩短管理员首次响应时间（指标在运营阶段补齐）。

## 范围
- In scope：
  - 双路径入口：
    - 代打服务：`/quote?type=service`（必须上传附件）
    - 平台产品：`/quote?type=product&productId=...&skuId=...`（无需上传附件）
  - 表单字段：姓名/邮箱/电话/公司、用途/数量/材料/工艺/精度/交期偏好、备注。
  - 报价预估：调用 `POST /api/quote/estimate` 输出区间与免责声明（仅供参考）。
  - 提交询价：调用 `POST /api/inquiries` 创建线索，返回线索编号与状态。
  - 提交成功回执：展示线索编号、预计响应说明（文案可后续配置）。
- Out of scope：
  - 邮件回执（可选增强）。
  - 复杂的模型体积/尺寸自动解析（可后续扩展到模型指标）。

## 用户故事
- 作为访客，我想通过上传文件或选择 SKU 提交询价，并得到一个可追踪编号，从而知道已提交成功并等待响应。

## 验收标准（必须可测试）
- [ ] 访问 `/quote?type=service`，不上传附件时无法提交（按钮置灰或提示）。
- [ ] 访问 `/quote?type=product&productId=...`，在缺少 `skuId` 时给出明确提示且无法提交。
- [ ] 点击“预估报价”会调用 `POST /api/quote/estimate`，展示 `priceMin/priceMax/currency/disclaimer`。
- [ ] 点击“提交询价”会调用 `POST /api/inquiries`，成功后展示线索 `id` 与成功提示。
- [ ] 表单存在 loading/error 状态：接口失败时给出可理解错误信息，不白屏。

## 边界与反例用例
- 输入校验：email 为空/格式不合法；quantity <= 0；超长备注；特殊字符。
- 上传：0 个文件、超大文件、非法扩展名、上传中断重试（前后端限制一致）。
- 估价：估价接口失败时允许直接提交询价（是否允许由 Tech Spec 定稿；默认允许提交但提示“无法预估”）。
- 反复点击：提交按钮需要防重复提交（前端 submitting 状态 + 后端幂等策略后续补齐）。

## 依赖与约束
- 相关入口：
  - 页面：`workspace/src/pages/Quote.tsx`
  - 子组件：`workspace/src/pages/quote/QuoteMainCard.tsx`、`workspace/src/pages/quote/QuoteSidebar.tsx`
  - API：`POST /api/quote/estimate`、`POST /api/inquiries`
  - 上传：`/api/uploads/presign`（如启用 COS 直传）

## 实现拆分（最小可回滚）
- 子任务 1：双路径参数规范（type/productId/skuId）与页面文案。
- 子任务 2：估价接口契约与错误态（estimate 可展示/可清除）。
- 子任务 3：提交接口契约与成功回执（含线索编号）。
- 子任务 4：上传约束与错误处理（类型/大小/重试）。

## 验证方式
- 自动化：`npm run check`、`npm run lint`、`npm run build`
- 手动路径：
  - `/quote?type=service`：不上传→不可提交；上传后→可提交
  - `/quote?type=product&productId=<id>&skuId=<id>`：可预估、可提交
  - 接口故障模拟：断网或返回 500 时错误展示合理

## 发布与回滚
- 发布检查点：两条路径均可走通；估价与提交接口可用；错误态无白屏。
- 回滚方式：如涉及接口契约变更，前后端需一起回滚；数据库写入需配合备份与迁移回滚策略。

