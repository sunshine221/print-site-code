# Tech Spec：FE-002 代打服务详情

## 1. 目标与非目标
- 目标：实现代打服务详情展示与清晰 CTA，引导进入 `/quote?type=service`。
- 非目标：复杂报价计算器与可视化规则编辑。

## 2. 现状说明（基于代码）
- 相关入口：
  - 页面：`workspace/src/pages/ServicePrint.tsx`
  - 路由：`workspace/src/App.tsx`
- 现有行为：页面已存在（具体内容以当前实现为准）。
- 现有问题：待补齐（结构化信息、FAQ、CTA 可见性等）。

## 3. 设计概览
- 方案摘要：页面以静态内容为主，后续可从站点设置加载可配置文案。
- 关键决策：CTA 跳转参数统一为 `type=service`。

## 4. API 契约（如涉及）
- 默认不新增 API；后续如需要“服务内容配置”，在 AD-004 站点设置中提供读取接口。

## 5. 前端交互（如涉及）
- 页面/路由：`/service/print`
- 交互：
  - CTA “上传文件获取报价” → `/quote?type=service`
  - CTA “联系咨询” → `/quote` 或站点联系信息锚点（待定）

## 6. 测试计划（必须覆盖边界）
- 手动回归路径：
  - `/service/print` 页面内容完整、移动端可用
  - CTA 跳转到 `/quote?type=service`

## 7. 发布与监控
- 发布步骤：前端静态资源发布。

